<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fullscreen AR rack-overlay activity. Launched by RackARPlugin.start().
             Uses AppTheme.AR (plain black) instead of the splash theme so the
             RackTrack logo doesn't bleed through behind the GL camera backdrop. -->
        <activity
            android:name=".RackARActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/AppTheme.AR"
            android:screenOrientation="portrait" />

        <!-- ARCore: tells Google Play to gate downloads to AR-capable devices.
             Use 'optional' if AR is a feature, not a requirement of the app. -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- ARCore needs OpenGL ES 3.0+. Marked not-required so non-AR
         users can still install the app and use the regular scan flow. -->
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />
</manifest>
